services:
  wordpress:
    build: .
    container_name: wordpress_app
    restart: no
    ports:
      - "${WP_PORT:-8081}:80"
    env_file:
      - .env
    networks:
      - red_global

networks:
  red_global:
    external: true

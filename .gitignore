# Credenciales — NUNCA commitear
# Se ignora cualquier variante (.env.local, .env.production, .env.*.bak, etc.)
# salvo la plantilla sin secretos.
.env
.env.*
!.env.example

# wp-config.php contiene los salts (AUTH_KEY, etc.). NUNCA se versiona.
# En el repo solo va la plantilla wp-config-sample.php.
wp-config.php
wp-config-local.php

# WordPress uploads y cache
wp-content/uploads/
wp-content/cache/
wp-content/upgrade/
wp-content/backup-db/

# Logs
*.log
debug.log

# OS
.DS_Store
Thumbs.db

# Respaldos comprimidos de temas
wp-content/themes/*.rar
wp-content/themes/*.zip
